{
  "name": "@cta-tracker/worker",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn workspace @cta-tracker/lib build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "yarn build && vitest run",
    "typecheck": "yarn build && tsc -p tsconfig.json",
    "types": "wrangler types --strict-vars=false"
  },
  "dependencies": {
    "@cta-tracker/lib": "workspace:^"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.128.0"
  }
}
